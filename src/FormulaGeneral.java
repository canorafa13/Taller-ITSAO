public class FormulaGeneral {
    public static void main(String args[]){
        int a = 1;
        int b = 2;
        int c = 1;

        double xPos = (-b + Math.pow((Math.pow(b, 2) - 4*a*c), 0.5))/(2*a);
        double xNeg = (-b - Math.pow((Math.pow(b, 2) - 4*a*c), 0.5))/(2*a);}
}
